import { IChatroomKitCore } from './types';
import { ChatroomMember, ChatroomMessage } from 'nim-web-sdk-ng/dist/CHATROOM_BROWSER_SDK/types';
export declare class ChatroomKitCore implements IChatroomKitCore {
    get version(): string;
    connect(): Promise<void>;
    destroy(): Promise<void>;
    disconnect(): Promise<void>;
    emit(): boolean;
    getChatroom(): void;
    off(): this;
    on(): this;
    queryMembers(): Promise<ChatroomMember[]>;
    queryMessageHistory(): Promise<ChatroomMessage[]>;
    sendFileMsg(): Promise<ChatroomMessage>;
    sendTextMsg(): Promise<ChatroomMessage>;
    setOptions(): void;
    updateMyRoomRole(): Promise<void>;
}
