import { NimKitCoreTypes } from '@xkit-yx/core-kit';
import RootStore from '.';
/**Mobx 可观察对象，负责 IM 的事件订阅与发布 */
export declare class EventStore {
    private rootStore;
    private nim;
    stateMap: Map<string, "online" | "offline">;
    constructor(rootStore: RootStore, nim: NimKitCoreTypes.INimKitCore);
    /** 销毁 EventStore，并取消相关的监听 */
    destroy(): void;
    /**
     * 订阅登录状态
     * @param accounts 账号
     */
    subscribeLoginStateActive(accounts: string[]): Promise<void>;
    private _onPushEvents;
}
