import { NimKitCoreTypes } from '@xkit-yx/core-kit';
import { FriendProfile } from 'nim-web-sdk-ng/dist/NIM_BROWSER_SDK/FriendServiceInterface';
import RootStore from '.';
export declare enum SyncFriendActionType {
    addFriend = 0,
    applyFriend = 1,
    passFriendApply = 2,
    rejectFriendApply = 3,
    deleteFriend = 4,
    updateFriend = 5
}
/**Mobx 可观察对象，负责管理好友信息的子 store */
export declare class FriendStore {
    private rootStore;
    private nim;
    friends: Map<string, FriendProfile>;
    constructor(rootStore: RootStore, nim: NimKitCoreTypes.INimKitCore);
    /**
     * 销毁FriendStore，会取消好友事件监听
     */
    destroy(): void;
    /**
     * 通过好友申请
     * @param account 账号
     */
    passFriendApplyActive(account: string): Promise<void>;
    /**
     * 拒绝好友申请
     * @param account 账号
     */
    rejectFriendApplyActive(account: string): Promise<void>;
    /**
     * 直接添加好友
     * @param account 账号
     */
    addFriendActive(account: string): Promise<void>;
    /**
     * 添加好友申请
     * @param account 账号
     */
    applyFriendActive(account: string): Promise<void>;
    /**
     * 删除好友
     * @param account 账号
     */
    deleteFriendActive(account: string): Promise<void>;
    /**
     * 更新好友信息
     * @param account 账号
     * @param account 备注
     */
    updateFriendActive(account: string, alias: string): Promise<void>;
    /**
     * FriendStore添加好友处理函数（FriendStore内部使用，外层不太推荐直接使用）
     * @param data - 好友数组
     */
    addFriend(data: FriendProfile[]): void;
    /**
     * FriendStore删除好友处理函数（FriendStore内部使用，外层不太推荐直接使用）
     * @param data - 好友数组
     */
    removeFriend(accounts: string[]): void;
    private _onFriends;
    private _onSyncFriend;
}
