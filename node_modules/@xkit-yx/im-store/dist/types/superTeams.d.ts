import { NimKitCoreTypes } from '@xkit-yx/core-kit';
import { SuperTeam } from 'nim-web-sdk-ng/dist/NIM_BROWSER_SDK/SuperTeamServiceInterface';
import RootStore from '.';
export declare class SuperTeamStore {
    private rootStore;
    private nim;
    superTeams: Map<string, SuperTeam>;
    constructor(rootStore: RootStore, nim: NimKitCoreTypes.INimKitCore);
    destroy(): void;
    addSuperTeam(superTeams: SuperTeam[]): void;
    removeSuperTeam(teamIds: string[]): void;
    private _onDismissSuperTeam;
    private _onUpdateSuperTeam;
    private _onSuperTeams;
    private _onTransferSuperTeam;
}
