import { NimKitCoreTypes } from '@xkit-yx/core-kit';
import { SuperTeamMember } from 'nim-web-sdk-ng/dist/NIM_BROWSER_SDK/SuperTeamServiceInterface';
import RootStore from '.';
export declare class SuperTeamMemberStore {
    private rootStore;
    private nim;
    teamMembers: Map<string, Map<string, SuperTeamMember>>;
    constructor(rootStore: RootStore, nim: NimKitCoreTypes.INimKitCore);
    destroy(): void;
    addSuperTeamMember(teamId: string, members: SuperTeamMember[]): void;
    removeSuperTeamMember(teamId: string, accounts?: string[]): void;
    updateSuperTeamMember(teamId: string, members: Partial<SuperTeamMember>[]): void;
    private _onAddSuperTeamMembers;
    private _onRemoveSuperTeamMembers;
    private _onUpdateSuperTeamManagers;
    private _onUpdateSuperTeamMember;
    private _onUpdateSuperTeamMembersMute;
}
