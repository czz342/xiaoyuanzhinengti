# 跑腿代办功能实现说明

## 功能概述

跑腿代办模块是一个完整的校园服务功能，包含以下核心功能：

1. **发布需求** - 学生可以发布跑腿代办需求
2. **我的订单** - 查看和管理自己发布的订单
3. **可接订单** - 浏览和接取其他学生发布的订单

## 前端页面结构

### 1. 页面布局
- **顶部导航栏**：返回按钮、标题、菜单按钮
- **功能切换标签**：三个标签页（发布需求、我的订单、可接订单）
- **内容区域**：根据选中的标签显示不同内容

### 2. 发布需求页面
- **服务类型选择**：外卖代拿、快递代取、小事代办
- **基本信息**：标题、详细描述
- **地点信息**：取件地点、送达地点
- **费用和时间**：跑腿费用、期望完成时间
- **联系方式**：联系电话
- **发布按钮**：提交需求

### 3. 我的订单页面
- **订单列表**：显示用户发布的所有订单
- **订单信息**：类型、状态、标题、描述、地点、费用
- **操作按钮**：取消订单、评价订单等

### 4. 可接订单页面
- **筛选栏**：按服务类型、价格范围筛选
- **订单列表**：显示可接取的订单
- **订单详情**：包含发布者信息、评分等
- **接单按钮**：接取订单
- **刷新按钮**：刷新订单列表

## 在Codewave上的实现步骤

### 第一步：创建数据实体

#### 1.1 跑腿订单实体 (ErrandOrder)
```
- id: 主键 (Integer)
- title: 标题 (String)
- description: 详细描述 (String)
- serviceType: 服务类型 (String)
- price: 跑腿费用 (Decimal)
- status: 订单状态 (String)
- publisherId: 发布者ID (String)
- accepterId: 接单者ID (String)
- pickupLocation: 取件地点 (String)
- deliveryLocation: 送达地点 (String)
- expectedTime: 期望完成时间 (String)
- phone: 联系电话 (String)
- createdTime: 发布时间 (DateTime)
- acceptedTime: 接单时间 (DateTime)
- completedTime: 完成时间 (DateTime)
```

#### 1.2 用户实体 (User)
```
- id: 主键 (Integer)
- studentId: 学号 (String)
- name: 姓名 (String)
- phone: 联系电话 (String)
- avatar: 头像 (String)
- creditScore: 信用评分 (Decimal)
- status: 用户状态 (String)
```

#### 1.3 订单状态记录实体 (OrderStatusLog)
```
- id: 主键 (Integer)
- orderId: 订单ID (Integer)
- status: 状态 (String)
- operatorId: 操作者ID (String)
- remark: 备注说明 (String)
- createdTime: 操作时间 (DateTime)
```

### 第二步：创建服务端逻辑

#### 2.1 发布订单逻辑 (publishErrandOrder)
**输入参数：**
- title, description, serviceType, price, pickupLocation, deliveryLocation, expectedTime, phone

**逻辑流程：**
1. 验证输入参数
2. 创建新的ErrandOrder记录
3. 设置初始状态为"待接单"
4. 返回创建结果

#### 2.2 查询我的订单逻辑 (getMyOrders)
**输入参数：**
- userId

**逻辑流程：**
1. 根据userId查询ErrandOrder
2. 按创建时间倒序排列
3. 返回订单列表

#### 2.3 查询可接订单逻辑 (getAvailableOrders)
**输入参数：**
- serviceType (可选), priceRange (可选)

**逻辑流程：**
1. 查询状态为"待接单"的订单
2. 根据筛选条件过滤
3. 返回可接订单列表

#### 2.4 接单逻辑 (acceptOrder)
**输入参数：**
- orderId, accepterId

**逻辑流程：**
1. 验证订单状态
2. 更新订单状态为"已接单"
3. 记录接单时间
4. 创建状态变更记录

#### 2.5 取消订单逻辑 (cancelOrder)
**输入参数：**
- orderId

**逻辑流程：**
1. 验证订单状态
2. 更新订单状态为"已取消"
3. 创建状态变更记录

### 第三步：创建页面

#### 3.1 主页面结构
使用Codewave的页面设计器创建：
- 顶部导航栏
- 标签页切换组件
- 三个内容区域

#### 3.2 发布需求表单
- 服务类型选择组件
- 输入框组件（标题、描述、地点、费用、电话）
- 时间选择器组件
- 发布按钮

#### 3.3 订单列表组件
- 列表组件
- 订单卡片模板
- 状态标签
- 操作按钮

#### 3.4 筛选组件
- 下拉选择器
- 筛选条件显示

### 第四步：配置页面逻辑

#### 4.1 页面加载逻辑
- 调用getMyOrders获取我的订单
- 调用getAvailableOrders获取可接订单

#### 4.2 标签切换逻辑
- 监听标签点击事件
- 切换显示内容

#### 4.3 表单提交逻辑
- 验证表单数据
- 调用publishErrandOrder
- 显示结果提示

#### 4.4 接单逻辑
- 确认接单对话框
- 调用acceptOrder
- 更新页面数据

### 第五步：样式和交互

#### 5.1 响应式布局
- 使用Codewave的布局组件
- 确保在不同设备上的显示效果

#### 5.2 状态管理
- 订单状态的颜色区分
- 按钮的启用/禁用状态

#### 5.3 用户体验
- 加载状态提示
- 操作成功/失败提示
- 表单验证提示

## 关键技术点

### 1. 数据绑定
- 使用Codewave的数据绑定功能
- 实现表单数据的双向绑定

### 2. 条件渲染
- 根据订单状态显示不同的操作按钮
- 根据用户角色显示不同的功能

### 3. 事件处理
- 表单提交事件
- 按钮点击事件
- 标签切换事件

### 4. 数据验证
- 前端表单验证
- 服务端数据验证

## 测试要点

### 1. 功能测试
- 发布需求功能
- 订单管理功能
- 接单功能
- 筛选功能

### 2. 数据测试
- 数据完整性
- 数据一致性
- 数据安全性

### 3. 用户体验测试
- 页面响应速度
- 操作流程顺畅性
- 错误提示友好性

## 部署注意事项

### 1. 权限控制
- 确保只有登录用户可以发布和接单
- 订单只能被发布者或接单者操作

### 2. 数据安全
- 敏感信息加密存储
- 防止SQL注入和XSS攻击

### 3. 性能优化
- 合理使用数据库索引
- 实现分页加载
- 缓存常用数据

## 总结

跑腿代办功能是一个完整的校园服务模块，通过Codewave平台可以快速实现。关键是要合理设计数据模型，创建必要的服务端逻辑，并设计良好的用户界面。整个功能涵盖了发布、管理、接单等完整的业务流程，为学生提供了便捷的校园服务体验。
